---
name: change-background
description: 换背景功能 — 将原图人物无缝融入新背景场景。支持两种模式：「氛围融合」通过反推背景特征生成自然语言提示词再重绘；「精准替换」直接双图融合一步到位。当用户提到换背景、替换背景、更换场景、背景融合、场景合成、抠图换景、改变拍摄环境等需求时，请使用此 skill。即使用户没有明确说"换背景"，只要涉及把人物放到新环境/新场景中，也应触发。
---

# 换背景功能

将原图中的人物主体提取出来，融入到全新的背景场景中，同时保持人物五官、发型、服装等全部特征不变。

本功能提供两种模式，适用于不同场景需求。开发者根据前端用户的选择来决定调用哪条链路。

---

## 模式一：氛围融合

适合目标背景是一张参考图、但希望最终效果更具"氛围感"的场景。流程分两步：先用文本模型反推背景特征，再用图像模型结合文字描述生成新环境。

### 前端传入的全局变量

- `VAR_IMAGE_A`：原 Look 图 URL（画布上包含主体人物的原图）
- `VAR_IMAGE_B`：目标背景图 URL（面板中选择的背景素材）
- `VAR_BATCH_SIZE`：生成张数（用户在前端面板选择的输出数量）

### Step 1：反推目标背景特征

- **调用 API**：`gemini-3.1-pro-preview`
- **图片 URL**：`[VAR_IMAGE_B]`（仅需分析背景图）
- **生成张数**：`1`
- **提示词 (Prompt)**：

```
##角色与任务
你是一个专业的AI图像场景提取器。请分析上传的图像，完全无视并剔除图像中的任何前景主体（人物、动物、核心物件），仅根据画面背景推导并生成一句用于AI生图的纯场景中文提示词。

##核心规则
1. **绝对禁令（屏蔽主体）**：严禁在输出中包含任何关于原图主体的描述。假装主体完全不存在，只描述它背后的纯粹场景。
2. **三要素融合**：这句话必须高度浓缩并包含以下细节：
   - **环境描述**（具体地点、建筑或自然景观，如"复古咖啡馆的角落"、"空旷的赛博朋克街道"）。
   - **空间关系**（景深、开阔度或物体间的排列，如"强烈的延伸透视感"、"纵深感极强的狭窄走廊"）。
   - **光影氛围**（光线质感、时间段、色彩基调，如"温暖的午后斜阳穿透窗户"、"清冷忧郁的阴天漫反射光"）。
3. **格式限制**：仅输出一句连贯的中文长句，不需要任何前导词（如"这是....."或"背景是..."），严禁输出任何解释性文字或markdown代码块。直接输出那句中文即可。
```

- **节点输出**：将大模型返回的纯文本结果存为变量 `VAR_SCENE_PROMPT`。

### Step 2：结合文本描述生成新环境

- **调用 API**：`gemini-3.1-flash-image-preview`
- **图片 URL**：`[VAR_IMAGE_A]`（此处仅需原图主体，背景由文本生成）
- **生成张数**：`{{VAR_BATCH_SIZE}}`
- **开发必读 — 参数动态注入逻辑**：在发送此步请求前，后端代码必须对 Step 1 返回的 `VAR_SCENE_PROMPT` 字符串进行 JSON 格式安全转义，并作为字符串值注入到下方 Prompt 的 `target_background` 字段中。
- **提示词 (Prompt 骨架)**：

```json
{
  "task": "移除原图背景，并根据文字描述生成新环境。",
  "target_background": "【此处由后端代码动态注入 VAR_SCENE_PROMPT 的安全转义字符串】",
  "directives": {
    "action": "保留原图人物主体，将其置入新生成的背景场景中。",
    "strict_keep": [
      "人物的五官、发型和体型必须 100% 保持不变。",
      "人物的服装、鞋子和配饰必须 100% 保持不变。"
    ],
    "adaptive_pose": "优先保持原姿势。若与新背景的物理空间产生冲突，允许微调肢体动作和朝向，确保人物自然站立或接触，消除悬浮感。",
    "blending": "根据新背景的光源对人物重新打光，并生成真实的接触阴影。"
  }
}
```

- **节点输出**：将返回的图片（数组）直接吐给前端展示。

---

## 模式二：精准替换

适合直接将人物"搬"到目标背景图中、追求高还原度的场景。一步到位，纯图生图逻辑，无需中间文本推理。

### 前端传入的全局变量

- `VAR_IMAGE_A`：原 Look 图 URL（包含主体人物的垫图）
- `VAR_IMAGE_B`：目标背景图 URL（作为场景源图）
- `VAR_BATCH_SIZE`：生成张数（用户在前端面板选择的输出数量）

### Step 1：双图融合的精准替换

- **调用 API**：`gemini-3.1-flash-image-preview`
- **图片 URL**：`[VAR_IMAGE_A, VAR_IMAGE_B]`
- **生成张数**：`{{VAR_BATCH_SIZE}}`
- **提示词 (Prompt)**：

```json
{
  "task_description": "使用'图2'中的背景环境，全面替换'图1'中的背景。",
  "generation_parameters": {
    "core_action": "识别并移除'图1'中的所有背景元素，仅保留其主体人物。然后，将'图2'中的完整场景景观作为新的背景植入到保留的人物身后。",
    "hard_constraints": [
      "绝对保持人物特征：严格保持原'图1'人物的面部五官、发型、发色、身材体型以及原始神态完全不变。",
      "绝对保持穿搭一致：保持原'图1'人物穿着的服装、鞋子、服饰材质以及所有配饰细节完全不变。"
    ],
    "dynamic_constraints": {
      "primary_goal": "在替换背景的过程中，默认尽可能保持'图1'中人物的原始姿势。",
      "adaptation_condition": "如果'图1'人物的原始姿势与'图2'新背景的空间透视、物理结构产生冲突，必须优先保证画面的空间合理性和逻辑性。",
      "allowed_adjustments": "允许对人物的肢体动作、朝向角度进行轻微、适度的调整，以确保人物能自然、真实地固定在新背景的空间关系中，避免出现'抠图贴纸'感。"
    },
    "technical_blending": [
      "光影重构：根据'图2'环境的光源方向、强弱和色温，对'图1'的人物主体进行全面的重新打光，实现环境光融合。",
      "物理接触：在人物与'图2'中地面或物体的接触处，精准生成符合新背景光影的接触阴影，确保主体扎根在场景中。"
    ]
  }
}
```

- **节点输出**：将返回的图片（数组）直接吐给前端展示。
